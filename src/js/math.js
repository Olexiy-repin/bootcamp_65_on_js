export const sum = (a, b) => {
  return a + b;
};

export const division = (a, b) => {
  return a / b;
};

export const subtraction = (a, b) => {
  return a - b;
};

export const multiply = (a, b) => {
  return a * b;
};

export default {
  division,
  sum,
};
